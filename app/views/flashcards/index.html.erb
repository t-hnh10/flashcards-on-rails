<table>
  <tr>
    <th>Term</th>
    <th>Transliteration</th>
    <th>Definition</th>
  </tr>
  <% @flashcards.each do |card| %>
    <tr>
      <td><%= card.front%></td>
      <td><%= card.transliteration%></td>
      <td><%= card.back%></td>
    </tr>
  <%end%>
  <p><a href='flashcards/new'>Add a new flashcard</a></p>
</div>
